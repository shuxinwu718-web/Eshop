<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shopsphere.eshop.mapper.CategoryMapper">

    <!-- 如果需要定义结果映射，可以加，但不是必须 -->
    <resultMap id="BaseResultMap" type="com.shopsphere.eshop.entity.Category">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="parent_id" property="parentId" />
        <result column="level" property="level" />
        <result column="sort" property="sortOrder" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT
            id, name, parent_id, level, sort, status, create_time, update_time, deleted
        FROM category
        WHERE parent_id = #{parentId}
          AND deleted = 0
        ORDER BY sort ASC, id ASC
    </select>
</mapper>